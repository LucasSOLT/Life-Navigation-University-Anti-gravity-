"use client";

import React from "react";
import { motion } from "framer-motion";

const Mission = () => {
    return (
        <section className="relative w-full h-screen bg-[#ececec] overflow-hidden">
            {/* Blank transition section */}
        </section>
    );
};

export default Mission;
